<Window x:Class="ScanProcedure.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        
        xmlns:local="clr-namespace:ScanProcedure.Views"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:views="clr-namespace:ScanProcedure.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="640" Width="800"
        MinHeight="350" MinWidth="600"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="12" GlassFrameThickness="0" ResizeBorderThickness="{x:Static SystemParameters.WindowResizeBorderThickness}" UseAeroCaptionButtons="False" CornerRadius="8"/>
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <!-- Centralized styles instead of repeating inline attributes -->
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Buttons.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0">
            <local:TitleBar/>
        </Border>
        <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}">
            <TabItem Header="General">
                <views:ScanProcedureView/>
            </TabItem>

            <TabItem Header="Advanced">
                <StackPanel Margin="10">
                    <TextBlock Text="Advanced Settings"/>
                    <CheckBox Content="Enable logging"
                          Margin="0,10,0,0"/>
                </StackPanel>
            </TabItem>

            <TabItem Header="About">
                <TextBlock Text="About this application"
                       Margin="10"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>