<Page x:Class="ScanProcedure.Views.MappingView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ScanProcedure.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MappingView">
    <Page.Resources>
        <Style TargetType="Button" x:Name="navBtn">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="8, 2" />
            <Setter Property="Padding" Value="12,8" />

        </Style>
    </Page.Resources>

    <DockPanel>
        <Grid DockPanel.Dock="Top" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="previous"/>
            <TextBlock Grid.Column="1" Text="Mapping" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button Grid.Column="2" Content="next"/>
        </Grid>
        <Grid>
            
        </Grid>

    </DockPanel>
</Page>
