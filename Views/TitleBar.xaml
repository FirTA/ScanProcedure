<UserControl x:Class="ScanProcedure.Views.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ScanProcedure.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Buttons.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Grid.Row="0" Background="{StaticResource WindowBackgroundBrush}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Scan Procedure" Foreground="White" VerticalAlignment="Center"
                           Margin="10,0,0,0"/>

        <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Button Content="—" Width="40" Click="Minimize_Click" Style="{StaticResource TitleBarButton}"/>
            <Button x:Name="MaxWindow" Content="☐" Width="40" Click="Maximize_Click" Style="{StaticResource TitleBarButton}"/>
            <Button x:Name="NormalWindow" Content="O" Width="40" Click="Normal_Click" Style="{StaticResource TitleBarButton}" Visibility="Collapsed"/>
            <Button Content="✕" Width="40" Click="Close_Click" Style="{StaticResource TitleBarButton}"/>
        </StackPanel>
    </Grid>
</UserControl>
